//8. Write a C program to input the length and width of a rectangle and find its perimeter.
#include<stdio.h>
void main()
{
	float per,len=10,bred=15;
	per=2*(len+bred);
	printf("are of rectangle will be per %f",per);
}
